{
  "buildCommand": "cd gdstats && npm install && npm run build",
  "outputDirectory": "gdstats/dist",
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/$1" },
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
